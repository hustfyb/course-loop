'use client';
import {useState,useEffect,useRef,useCallback} from 'react';
import {BookOpen,Layers3,Users,ClipboardCheck,Settings2,Plus,ArrowUp,Paperclip,ArrowUpRight,Sparkles,ChevronRight,FileText,CircleHelp,LogOut,Mail,Check,Download,RefreshCw,Send,ShieldCheck,Clock,Copy, X, LoaderCircle} from 'lucide-react';
import {SidebarProvider,Sidebar,SidebarHeader,SidebarContent,SidebarFooter,SidebarMenu,SidebarMenuItem,SidebarMenuButton,SidebarGroup,SidebarGroupLabel,SidebarInset,SidebarTrigger} from '@/components/ui/sidebar';
import {Button} from '@/components/ui/button';
import {Input} from '@/components/ui/input';
import {Dialog,DialogContent,DialogHeader,DialogTitle,DialogDescription,DialogFooter} from '@/components/ui/dialog';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
import {Table,TableHeader,TableBody,TableHead,TableRow,TableCell} from '@/components/ui/table';
import {Switch} from '@/components/ui/switch';
import {initialCourse} from '@/lib/seed';
import {statusNames} from '@/lib/domain';
type Any=any;
const fmt=(n:number)=>new Date(n).toLocaleString('zh-CN',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'});
async function call(path:string,data?:Any){const r=await fetch('/api/'+path,{method:data===undefined?'GET':'POST',headers:data instanceof FormData?{}:{'Content-Type':'application/json'},body:data===undefined?undefined:data instanceof FormData?data:JSON.stringify(data)});const v:Any=await r.json();if(!r.ok)throw Error(v.error||'请求未完成');return v;}
const navs=[['studio','课程工作台',Sparkles],['courses','实验与发布',BookOpen],['teams','班级与 Team',Users],['reports','提交与评估',ClipboardCheck],['settings','连接与设置',Settings2]] as const;
function Status({value}:{value:string}){return <span className={'tag '+(['complete','published','accepted'].includes(value)?'green':['failed','open'].includes(value)?'amber':'blue')}>{statusNames[value]||value}</span>}
function Empty({title,body,children}:Any){return <div className="empty-state"><div className="agent-symbol"><Layers3 size={25}/></div><h2>{title}</h2><p>{body}</p>{children}</div>}
export default function Workbench(){
 const [s,setS]=useState<Any>({user:null,courses:[]});const [loaded,setLoaded]=useState(false);const [view,setView]=useState('studio');const [selected,setSelected]=useState('');const [message,setMessage]=useState('');const [attachments,setAttachments]=useState<Any[]>([]);const [busy,setBusy]=useState(false);const [notice,setNotice]=useState('');const [error,setError]=useState('');const [modal,setModal]=useState<Any>(null);const [form,setForm]=useState<Any>({});const [detail,setDetail]=useState<Any>(null);const [token,setToken]=useState('');const uploadRef=useRef<HTMLInputElement>(null);
 const refresh=useCallback(async()=>{try{const state=await call('state'+(selected?'?course='+encodeURIComponent(selected):''));setS(state);if(state.user?.role==="student")setView(v=>["studio","settings"].includes(v)?"courses":v);setLoaded(true);}catch(e:Any){setError(e.message);setLoaded(true);}},[selected]);
 useEffect(()=>{refresh();},[refresh]);useEffect(()=>{if(!s.user)return;const t=setInterval(refresh,7000);return()=>clearInterval(t)},[refresh,s.user?.id]);
 const course=s.courses?.find((c:Any)=>c.id===s.selected);const teacher=s.user?.role==='teacher';const draft=s.draft||initialCourse;const team=s.teams?.find((t:Any)=>t.id===s.myTeam);const currentNav=navs.find(n=>n[0]===view)!;
 const open=(type:string,data:Any={})=>{setForm({});setModal({type,...data});setError('');};
 async function act(fn:()=>Promise<Any>){setBusy(true);setError('');try{await fn();await refresh();}catch(e:Any){setError(e.message);}finally{setBusy(false)}}
 async function mutation(path:string,data:Any,success='已保存'){await act(async()=>{await call(path,data);setNotice(success);setModal(null);});}
 async function ensureCourse(){if(course)return course.id;const r=await call('courses',{});setSelected(r.id);return r.id;}
 async function upload(files:FileList|null,student=false){if(!files?.length)return;if(!s.user){open('login');return;}await act(async()=>{const cid=student?s.selected:await ensureCourse();for(const file of Array.from(files)){const data=new FormData();data.set('courseId',cid);data.set('file',file);if(student&&team)data.set('teamId',team.id);const f=await call('upload',data);setAttachments(a=>[...a,f]);}setNotice('文件已上传，可随消息或作业一起提交。');});}
 const go=(id:string)=>{setView(id);setDetail(null);setAttachments([]);setMessage('');};
 const changes=s.publishedDraft?draft.experiments.filter((e:Any)=>JSON.stringify(e)!==JSON.stringify(s.publishedDraft.experiments.find((p:Any)=>p.id===e.id))).map((e:Any)=>e.title):draft.experiments.map((e:Any)=>e.title);
 const cp=(v:string)=>act(async()=>{await navigator.clipboard.writeText(v);setNotice('已复制');});
 async function sendChat(){if(!s.user){open('login');return;}await act(async()=>{const cid=await ensureCourse();await call('chat',{courseId:cid,message:message||'请根据附件整理实验任务与评分草案，不明确的事项请列出问题。',fileIds:attachments.map(a=>a.id)});setMessage('');setAttachments([]);setNotice('已进入课程分析队列，连接器接入后开始处理。');});}
 const field=(key:string,value:Any)=>setForm((f:Any)=>({...f,[key]:value}));
 return <SidebarProvider><Sidebar className="app-sidebar"><SidebarHeader><a className="brand" href="/"><span className="brand-mark"><Layers3 size={24}/></span><span>课序<small>COURSE / LOOP</small></span></a></SidebarHeader><SidebarContent><SidebarGroup><SidebarGroupLabel>{teacher?'教学工作空间':'学习工作空间'}</SidebarGroupLabel><SidebarMenu>{navs.filter(n=>teacher||!s.user||!['studio','settings'].includes(n[0])).map(([id,label,Icon])=><SidebarMenuItem key={id}><SidebarMenuButton isActive={view===id} onClick={()=>go(id)}><Icon/><span>{id==='teams'&&!teacher?'我的 Team':label}</span></SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu></SidebarGroup><div className="sidebar-course"><span className="tiny-label">当前教学期</span>{s.courses?.length?<Select value={s.selected} onValueChange={v=>{setSelected(String(v));setAttachments([]);setDetail(null);}}><SelectTrigger className="course-select"><SelectValue/></SelectTrigger><SelectContent>{s.courses.map((c:Any)=><SelectItem key={c.id} value={c.id}>{c.title} · {c.term}</SelectItem>)}</SelectContent></Select>:<><strong>软件工程 3.0</strong><span>2026 秋季 · 4 次实验</span></>}<div className="course-line"><i/></div><small>从规格到可信交付</small></div></SidebarContent><SidebarFooter><div className="sidebar-person"><span className="avatar">{s.user?.name?.slice(0,1)||'课'}</span><div>{s.user?.name||'欢迎来到课序'}<small>{s.user?(teacher?'课程负责人':s.user.studentNo||'学生'):'登录后开始协作'}</small></div>{s.user?<button aria-label="退出登录" onClick={()=>act(async()=>{await call('auth/logout',{});setSelected('');setView('studio');})}><LogOut size={16}/></button>:<button onClick={()=>open('login')}>登录</button>}</div></SidebarFooter></Sidebar><SidebarInset><header className="topbar"><div><SidebarTrigger/><span>工作空间</span><ChevronRight size={15}/><strong>{currentNav[1]}</strong></div><div><span className={'connection '+(s.health?.online&&s.health?.acp?'online':'')}><i/>{s.health?.online&&s.health?.acp?'Pi 已连接':'Pi 尚未连接'}</span>{!s.user&&<Button size="sm" variant="outline" onClick={()=>open('login')}><Mail size={15}/>邮箱登录</Button>}</div></header><main className="workspace">{error&&<div className="feedback error" role="alert">{error}<button aria-label="关闭错误" onClick={()=>setError('')}><X size={16}/></button></div>}{notice&&<div className="feedback" role="status">{notice}<button aria-label="关闭提示" onClick={()=>setNotice('')}><X size={16}/></button></div>}{busy&&<div className="busy-indicator" role="status"><LoaderCircle size={16} className="spin"/>正在处理…</div>}
 <div className="page-heading"><div className="eyebrow">{view==='studio'?'COURSE STUDIO':view==='teams'?'LEARN TOGETHER':view==='reports'?'EVIDENCE & FEEDBACK':view==='settings'?'WORKSPACE CONNECTIONS':'EXPERIMENT LIBRARY'}<span>{course?.term||'2026 / AUTUMN'}</span></div><div className="heading-row"><div><h1>{view==='studio'?'把课程想法，变成学习任务。':view==='courses'?'每一次实验，都有明确的标准。':view==='teams'?'一起构建，一起交付。':view==='reports'?'让每一分，都有据可查。':'把你的 Agent 接入工作台。'}</h1><p>{view==='studio'?'交给 Agent 一份文档，在对话中完善，在发布前确认。':view==='courses'?'练习获得反馈，正式提交留下可追溯的成果。':view==='teams'?'使用同一个 Team 完成实验，正式提交会保存成员快照。':view==='reports'?'查看核验进度、分项反馈和需要你补充的证据。':'网站维护课程与成绩，Pi 负责文档理解和实验核验。'}</p></div>{teacher?<Button className="primary" onClick={()=>open('newCourse')}><Plus size={17}/>新建教学期</Button>:s.user?<Button onClick={()=>open('join')}><Plus size={17}/>加入课程</Button>:<Button className="primary" onClick={()=>open('login')}><ArrowUpRight size={17}/>进入工作空间</Button>}</div></div>
 {(!s.user||teacher)&&view==='studio'&&<div className="studio-grid"><section className="conversation panel"><div className="panel-title"><span><Sparkles size={18}/>与课程 Agent 协作</span><span className="tag">{course?`草案 v${course.revision}`:'草案阶段'}</span></div><div className="conversation-body">{s.messages?.length?<div className="messages">{s.messages.map((m:Any)=><article className={'message '+m.role} key={m.id}><div className="message-label">{m.role==='user'?'你':'课程 Agent'}<small>{fmt(m.created)}</small></div><p>{m.content}</p>{JSON.parse(m.fileIds||'[]').map((fid:string)=><a className="file-link" href={'/api/files/'+fid} key={fid}><Paperclip size={13}/>{s.files?.find((f:Any)=>f.id===fid)?.name||'附件'}</a>)}</article>)}</div>:<><div className="agent-symbol"><Sparkles size={23}/></div><h2>从你已有的材料开始</h2><p>上传教学大纲、实验指导或评分文档。Agent 会整理任务和评分项，把需要你决定的地方留在这里。</p><div className="starter-prompts"><button onClick={()=>setMessage('请检查这四次实验的任务、提交要求和评分标准是否一致。')}><FileText size={17}/>检查四次实验的一致性<ArrowUpRight size={15}/></button><button onClick={()=>setMessage('请根据我上传的课程文档生成实验任务和评分草案。')}><Layers3 size={17}/>从文档生成课程草案<ArrowUpRight size={15}/></button></div></>}{s.jobs?.filter((j:Any)=>j.status!=='complete').slice(0,3).map((j:Any)=><div className="job-note" key={j.id}><Status value={j.status}/><span>{j.error||'课程分析任务'}</span>{j.status==='failed'?<button onClick={()=>mutation('job-action',{id:j.id,action:'retry'})}>重试</button>:['queued','running'].includes(j.status)?<button onClick={()=>mutation('job-action',{id:j.id,action:'cancel'})}>取消</button>:null}</div>)}<div className="notice"><CircleHelp size={16}/><span>{s.health?.online?'有歧义时，Agent 会在这里追问。':'连接 Pi 后开始分析。四次实验已预置，可先预览和发布。'}</span></div></div><div className="composer">{attachments.length>0&&<div className="attachment-list">{attachments.map(f=><span key={f.id}><FileText size={13}/>{f.name}<button aria-label={'移除 '+f.name} onClick={()=>setAttachments(a=>a.filter(x=>x.id!==f.id))}><X size={12}/></button></span>)}</div>}<textarea aria-label="给课程 Agent 的消息" placeholder="描述你的想法，或附上一份文档…" value={message} onChange={e=>setMessage(e.target.value)}/><div><Button variant="ghost" size="sm" disabled={busy} onClick={()=>uploadRef.current?.click()}><Paperclip size={17}/>添加文档</Button><input hidden ref={uploadRef} type="file" multiple accept=".md,.txt,.docx,.pdf" onChange={e=>{upload(e.target.files);e.target.value='';}}/><button className="send" aria-label="发送给 Agent" disabled={busy||(!message.trim()&&!attachments.length)} onClick={sendChat}><ArrowUp size={18}/></button></div></div><div className="composer-hint">修改先进入草案，确认后才会发布给学生。</div></section><section className="course-preview"><div className="preview-heading"><span className="tiny-label">课程预览</span><span className="tag blue">{draft.experiments.length} 次实验</span></div><h2>{draft.title}</h2><p className="muted">{draft.description}</p><div className="course-facts"><span>32 学时</span><span>8 周</span><span>Team 协作</span></div>{draft.questions?.length>0&&<div className="questions"><strong>需要你确认</strong>{draft.questions.map((q:string,i:number)=><button key={i} onClick={()=>setMessage(`关于“${q}”，我的决定是：`)}>{i+1}. {q}<ArrowUpRight size={14}/></button>)}</div>}<div className="experiment-list">{draft.experiments.map((e:Any,i:number)=><button key={e.id} className="experiment-card" onClick={()=>{go('courses');setDetail(e);}}><div className="experiment-number">{String(i+1).padStart(2,'0')}</div><div><div className="experiment-meta">{e.week}<span>10 分</span></div><h3>{e.title}</h3><p>{e.summary}</p></div><ChevronRight size={17}/></button>)}</div><div className="preview-bottom"><span><span className="dot"/>{s.release?`已发布 v${s.release.revision}`:'预置课程 · 待发布'}</span><Button variant="outline" onClick={()=>go('courses')}>查看完整草案<ArrowUpRight size={16}/></Button></div></section></div>}
 {view==='courses'&&<>{s.files?.filter((f:Any)=>!f.teamId).length>0&&<section className="panel data-panel"><h2>课程文档与素材</h2>{s.files.filter((f:Any)=>!f.teamId).map((f:Any)=><div className="member" key={f.id}><FileText size={18}/><a className="file-link" href={'/api/files/'+f.id}>{f.name}</a><span className="tag">{f.visibility==='student'?'学生可见':'教师内部'}</span>{teacher&&<Button size="sm" variant="outline" onClick={()=>mutation('file-visibility',{id:f.id,shared:f.visibility!=='student'})}>{f.visibility==='student'?'设为内部':'共享给学生'}</Button>}</div>)}</section>}<div className="toolbar"><span className="muted">{teacher?'确认内容后发布，当前学生看到的是已发布版本。':s.release?'以下是教师已发布的实验要求。':'教师发布后即可开始练习。'}</span>{teacher&&<Button disabled={busy||!course||course.revision===s.release?.revision} onClick={()=>open('publish')}><Send size={16}/>预览并发布</Button>}</div><div className="lab-grid">{(s.user&&!teacher&&!s.release?[]:draft.experiments).map((e:Any,i:number)=><button className={'lab-card panel '+(detail?.id===e.id?'chosen':'')} key={e.id} onClick={()=>{setDetail(e);setAttachments([]);}}><div className="lab-top"><span className="experiment-number">0{i+1}</span><span className="tag">{e.week}</span></div><h2>{e.title}</h2><p>{e.summary}</p><div className="lab-bottom"><span>Team 提交</span><span>10 分<ArrowUpRight size={16}/></span></div></button>)}</div>{s.user&&!teacher&&!s.release&&<Empty title="实验尚未发布" body="加入课程后，等待教师确认并发布任务。"/>}{detail&&<section className="panel detail-panel"><div className="section-heading"><div><span className="tiny-label">EXPERIMENT DETAILS</span><h2>{detail.title}</h2></div><button aria-label="收起实验详情" onClick={()=>setDetail(null)}><X size={19}/></button></div><div className="detail-columns"><div><h3>任务与约束</h3><p className="prose-text">{detail.task}</p><h3>提交清单</h3><ul>{detail.deliverables.map((d:string)=><li key={d}>{d}</li>)}</ul><h3>课程素材</h3>{detail.sourceFiles?.map((f:string)=><a className="file-link" href={'/materials/'+f+'.zip'} key={f}><Download size={14}/>{f}.zip</a>)}</div><div><h3>评分标准</h3>{detail.rubric.map((r:Any)=><div className="rubric-row" key={r.id}><div><strong>{r.title}</strong><p>{r.criteria}</p></div><span>{r.max} 分</span></div>)}</div></div>{s.user&&!teacher&&<div className="submission-box"><h3>提交本组成果</h3>{!team?<p className="muted">请先在“我的 Team”创建或加入小组。</p>:<><p className="muted">{team.name} · 上传 ZIP、Markdown 或相关证据。单个文件不超过 20 MB。</p><input aria-label="上传作业文件" type="file" multiple onChange={e=>{upload(e.target.files,true);e.target.value='';}}/><div className="attachment-list">{attachments.map(f=><span key={f.id}>{f.name}<button aria-label="移除附件" onClick={()=>setAttachments(a=>a.filter(x=>x.id!==f.id))}><X size={12}/></button></span>)}</div><textarea className="text-area" placeholder="说明分工、运行方式及需要核验的内容" value={message} onChange={e=>setMessage(e.target.value)}/><div className="button-row"><Button variant="outline" disabled={busy||!attachments.length} onClick={()=>mutation('submit',{teamId:team.id,experimentId:detail.id,mode:'practice',fileIds:attachments.map(f=>f.id),note:message},'练习已进入核验队列')}>提交练习</Button><Button disabled={busy||!attachments.length||team.leader!==s.user.id} onClick={()=>open('formal')}>确认正式提交</Button><span className="muted">正式提交由组长确认，每个实验最多 {course?.maxFormal||2} 个正式版本。</span></div></>}</div>}</section>}</>}
 {view==='teams'&&(!s.user?<Empty title="用邮箱加入你的学习小组" body="注册后加入课程，创建 Team 或接受同学的邀请。"><Button onClick={()=>open('login')}>注册 / 登录</Button></Empty>:!course?<><Empty title="先加入一个教学班" body="输入课程邀请码，或接受下面发给你的 Team 邀请。"><Button onClick={()=>open('join')}>输入课程邀请码</Button></Empty>{s.pendingInvites?.map((iv:Any)=><div className="invite-banner" key={iv.id}><Mail size={20}/><div><strong>{iv.teamName} 邀请你加入</strong><p>接受后自动加入相应教学班。</p></div><Button variant="outline" onClick={()=>mutation('invite-action',{id:iv.id,action:'decline'})}>拒绝</Button><Button onClick={()=>mutation('invite-action',{id:iv.id,action:'accept'})}>接受邀请</Button></div>)}</>:<><div className="toolbar"><span>{teacher?`${s.students?.length||0} 位学生 · ${s.teams?.length||0} 个 Team`:`每组最多 ${course.maxSize} 人 · 同一期只能加入一个 Team`}</span>{teacher?<Button variant="outline" onClick={()=>cp(course.joinCode)}><Copy size={15}/>课程邀请码：{course.joinCode}</Button>:!team&&<Button onClick={()=>open('createTeam')}><Plus size={16}/>创建 Team</Button>}</div>{s.invites?.filter((i:Any)=>i.status==='pending'&&i.email===s.user.email).map((iv:Any)=><div className="invite-banner" key={iv.id}><Mail size={20}/><div><strong>{iv.teamName} 邀请你加入</strong><p>接受后将成为这个 Team 的成员。</p></div><Button variant="outline" onClick={()=>mutation('invite-action',{id:iv.id,action:'decline'})}>拒绝</Button><Button onClick={()=>mutation('invite-action',{id:iv.id,action:'accept'})}>接受邀请</Button></div>)}<div className="teams-grid">{s.teams?.map((t:Any)=><section className="panel team-card" key={t.id}><div className="section-heading"><div className="team-title"><span className="team-icon"><Users size={22}/></span><div><h2>{t.name}</h2><span className="muted">{t.members.length} / {course.maxSize} 人 {t.locked?'· 成员已锁定':''}</span></div></div>{(teacher||t.leader===s.user.id)&&<Button size="sm" variant="outline" disabled={!!t.locked&&!teacher} onClick={()=>open('invite',{teamId:t.id})}><Plus size={15}/>邀请</Button>}</div>{t.members.map((m:Any)=><div className="member" key={m.id}><span className="avatar">{m.name.slice(0,1)}</span><div><strong>{m.name}</strong><small>{m.email} · {m.studentNo}</small></div>{t.leader===m.id?<span className="tag blue">组长</span>:<>{(teacher||t.leader===s.user.id)&&<button className="text-action" disabled={!!t.locked&&!teacher} onClick={()=>open('teamConfirm',{teamId:t.id,userId:m.id,action:'transfer',label:`将组长转让给 ${m.name}`})}>转让</button>}{teacher&&<button className="text-action danger" onClick={()=>open('teamConfirm',{teamId:t.id,userId:m.id,action:'remove',label:`移除成员 ${m.name}`})}>移除</button>}</>}</div>)}{!teacher&&t.leader!==s.user.id&&<Button className="mt-4" variant="outline" disabled={!!t.locked} onClick={()=>open('teamConfirm',{teamId:t.id,action:'leave',label:'退出这个 Team'})}>退出小组</Button>}</section>)}</div>{!s.teams?.length&&<Empty title="还没有 Team" body={teacher?'学生加入课程后，可以自行组队并邀请同学。':'创建你的小组，或请组长使用你的邮箱发出邀请。'}/>}<section className="panel data-panel"><h2>邀请记录</h2><Table><TableHeader><TableRow><TableHead>Team</TableHead><TableHead>邀请邮箱</TableHead><TableHead>状态</TableHead><TableHead>操作</TableHead></TableRow></TableHeader><TableBody>{s.invites?.map((iv:Any)=><TableRow key={iv.id}><TableCell>{iv.teamName}</TableCell><TableCell>{iv.email}</TableCell><TableCell><Status value={iv.status}/></TableCell><TableCell>{iv.status==='pending'&&iv.email!==s.user.email&&<button className="text-action" onClick={()=>mutation('invite-action',{id:iv.id,action:'revoke'})}>撤销</button>}</TableCell></TableRow>)}</TableBody></Table>{!s.invites?.length&&<p className="muted py-4">暂无邀请记录</p>}</section>{teacher&&<section className="panel data-panel"><h2>教学班学生</h2><Table><TableHeader><TableRow><TableHead>姓名</TableHead><TableHead>学号</TableHead><TableHead>邮箱</TableHead><TableHead>组队状态</TableHead></TableRow></TableHeader><TableBody>{s.students?.map((m:Any)=><TableRow key={m.id}><TableCell>{m.name}</TableCell><TableCell>{m.studentNo}</TableCell><TableCell>{m.email}</TableCell><TableCell>{s.teams?.find((t:Any)=>t.id===m.teamId)?.name||'尚未组队'}</TableCell></TableRow>)}</TableBody></Table></section>}</>)}
 {view==='reports'&&<><div className="toolbar"><span className="muted">{s.submissions?.length||0} 次提交 · 系统异常不会直接计零分</span><div className="button-row"><Button variant="outline" size="sm" onClick={refresh}><RefreshCw size={14}/>刷新</Button>{teacher&&<Button variant="outline" size="sm" onClick={()=>{const rows=[['小组','实验','类型','版本','分数','状态'],...(s.submissions||[]).map((x:Any)=>[x.teamName,x.experimentId,x.mode,x.ordinal,x.report?.total??'',x.status])];const csv='\ufeff'+rows.map(row=>row.map((x:Any)=>'"'+String(x).replace(/^[=+@-]/,"'").replaceAll('"','""')+'"').join(',')).join('\r\n');const a=document.createElement('a');const u=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.href=u;a.download='实验成绩.csv';a.click();URL.revokeObjectURL(u);}}><Download size={14}/>导出成绩</Button>}</div></div>{!s.submissions?.length?<Empty title="还没有提交记录" body="小组上传练习或正式成果后，核验进度和分项反馈会出现在这里。"><Button variant="outline" onClick={()=>go('courses')}>查看实验任务</Button></Empty>:<div className="report-list">{s.submissions.map((sub:Any)=><section className="panel report-card" key={sub.id}><div className="section-heading"><div><div className="button-row"><span className="tag">{sub.mode==='practice'?'练习':'正式'} · v{sub.ordinal}{sub.superseded?' · 已被替代':''}</span><Status value={sub.status}/></div><h2>{draft.experiments.find((e:Any)=>e.id===sub.experimentId)?.title||sub.experimentId}</h2><p className="muted">{sub.teamName} · {fmt(sub.created)} · 快照成员：{sub.members.map((m:Any)=>m.name).join('、')}</p></div><div className="score">{sub.report?sub.report.total:'—'}<small>/ 10</small></div></div>{sub.error&&<div className="feedback error">{sub.error}</div>}{sub.report?<><div className="score-items">{sub.report.items.map((i:Any)=><div key={i.id}><strong>{draft.experiments.find((e:Any)=>e.id===sub.experimentId)?.rubric.find((r:Any)=>r.id===i.id)?.title||i.id}<span>{i.score} 分</span></strong><p>{i.reason}</p><small>证据：{i.evidence.join('；')}</small></div>)}</div>{sub.report.limitations?.length>0&&<div className="questions"><strong>尚未核验</strong>{sub.report.limitations.map((l:string,i:number)=><p key={i}>{l}</p>)}</div>}{sub.report.questions?.length>0&&<div className="questions"><strong>个人核验</strong>{sub.report.questions.map((q:string,i:number)=><p key={i}>{i+1}. {q}</p>)}{!teacher&&!sub.answers?.length&&<Button size="sm" onClick={()=>open('answer',{submissionId:sub.id})}>回答问题</Button>}{sub.answers?.map((a:Any)=><div className="answer-record" key={a.id}><p>{a.content}</p><Status value={a.status}/>{a.result?.feedback&&<p>{a.result.feedback}</p>}</div>)}</div>}</>:<p className="muted py-5">{sub.status==='complete'?'评分已生成，等待教师复核发布。':sub.status==='queued'?'等待 Pi 连接器领取任务。系统不会生成模拟分数。':'核验结果将在完成后展示。'}</p>}<div className="button-row">{sub.fileIds.map((fid:string)=><a className="file-link" href={'/api/files/'+fid} key={fid}><Download size={13}/>{s.files?.find((f:Any)=>f.id===fid)?.name||'提交文件'}</a>)}</div><div className="report-actions">{sub.status==='failed'&&<Button size="sm" variant="outline" onClick={()=>mutation('job-action',{id:sub.jobId,action:'retry'})}>重试核验</Button>}{teacher&&sub.report&&<Button size="sm" onClick={()=>open('review',{sub})}>{sub.published?'复核 / 调整':'复核并发布'}</Button>}{!teacher&&sub.report&&<Button size="sm" variant="outline" onClick={()=>open('appeal',{submissionId:sub.id})}>对评分提出申诉</Button>}</div></section>)}</div>}{s.appeals?.length>0&&<section className="panel data-panel"><h2>申诉与复核记录</h2>{s.appeals.map((a:Any)=><div className="appeal-row" key={a.id}><Status value={a.status}/><div><p>{a.content}</p>{a.resolution&&<small>处理意见：{a.resolution}</small>}</div>{teacher&&a.status==='open'&&<Button size="sm" variant="outline" onClick={()=>open('review',{sub:s.submissions.find((x:Any)=>x.id===a.submissionId),appealId:a.id})}>处理</Button>}</div>)}</section>}</>}
 {view==='settings'&&(teacher?<div className="settings-grid"><section className="panel settings-card"><div className="agent-symbol"><Sparkles size={23}/></div><h2>Pi Agent 连接器</h2><p>在你配置 Pi 的机器上运行连接器。它主动领取课程分析和核验任务，网站无需访问模型账号。</p><Status value={s.health?.online&&s.health?.acp?'complete':'queued'}/><div className="button-row"><Button onClick={()=>open('pair')}><Plus size={15}/>生成连接凭据</Button><a className="file-link" href="/course-loop-connector.zip" download><Download size={14}/>下载连接器</a><a className="file-link" href="/connector-guide.txt" download><Download size={14}/>连接说明</a></div>{token&&<div className="token-box"><strong>仅在本页显示，请保存到连接器配置</strong><code>{token}</code><Button variant="outline" size="sm" onClick={()=>cp(token)}>复制凭据</Button></div>}<div className="notice"><ShieldCheck size={16}/><span>每个评分任务使用独立目录。学生代码应在你配置的隔离环境运行。</span></div></section><section className="panel settings-card"><div className="agent-symbol"><Mail size={23}/></div><h2>邮箱注册与邀请</h2><p>连接器通过你的 SMTP 服务发送登录验证码和 Team 邀请。未连接时，网站会明确提示，验证码不会假装已发送。</p><Status value={s.health?.online&&s.health?.mail?'complete':'queued'}/><p className="muted">教师身份由部署时指定的管理员邮箱授权，学生不能自行切换教师权限。</p></section>{course&&<section className="panel settings-card"><h2>本期规则</h2><p>小组最多 {course.maxSize} 人 · 正式提交最多 {course.maxFormal} 个版本</p><p>截止时间：{course.deadline?fmt(course.deadline):'未设置'}</p><p>自动发布：{course.autoPublish?'已启用':'关闭，待教师校准后启用'}</p><Button variant="outline" onClick={()=>{open('settings');setForm({maxSize:course.maxSize,deadline:course.deadline?new Date(course.deadline-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16):'',autoPublish:!!course.autoPublish});}}>修改本期规则</Button></section>}</div>:<Empty title="教师连接设置" body="请使用已授权的管理员邮箱登录。"><Button onClick={()=>open('login')}>邮箱登录</Button></Empty>)}
 {!loaded&&<p className="muted" role="status">正在连接工作空间…</p>}<footer className="workspace-footer"><span>先定义标准，再验证成果。</span><span>练习反馈 / 正式核验 / 可追溯评分</span></footer></main></SidebarInset>
 <Dialog open={!!modal} onOpenChange={v=>{if(!v&&!busy)setModal(null)}}><DialogContent className="app-dialog"><DialogHeader><DialogTitle>{({login:'邮箱登录 / 注册',newCourse:'新建教学期',join:'加入课程',createTeam:'创建 Team',invite:'邀请同学加入',teamConfirm:'确认成员变更',publish:'确认发布课程',formal:'确认正式提交',answer:'个人理解核验',appeal:'对评分提出申诉',review:'复核与成绩发布',pair:'生成连接器凭据',settings:'本期规则'} as Any)[modal?.type]}</DialogTitle><DialogDescription>{modal?.type==='login'?'使用你的邮箱获取验证码，首次注册请填写姓名与学号。':modal?.type==='formal'?'本次将固定提交文件、评分规则和当前小组成员。原版本不会被覆盖。':modal?.type==='publish'?'学生将看到以下草案版本。之后的修改进入新版本，不改变历史提交依据。':'提交前请核对内容。'}</DialogDescription></DialogHeader>{error&&<p className="dialog-error" role="alert">{error}</p>}
 {modal?.type==='login'&&<><label>邮箱<Input autoComplete="email" type="email" value={form.email||''} onChange={e=>field('email',e.target.value)}/></label><div className="form-two"><label>姓名<Input value={form.name||''} onChange={e=>field('name',e.target.value)}/></label><label>学号<Input value={form.studentNo||''} onChange={e=>field('studentNo',e.target.value)}/></label></div><label>验证码<div className="button-row"><Input autoComplete="one-time-code" inputMode="numeric" maxLength={6} value={form.code||''} onChange={e=>field('code',e.target.value)}/><Button variant="outline" disabled={busy} onClick={()=>act(async()=>{const r=await call('auth/request',{email:form.email});setNotice(r.message);})}>获取验证码</Button></div></label>{s.local&&<p className="muted">当前为本地开发环境，使用部署者配置的开发验证码。</p>}<Button disabled={busy} onClick={()=>act(async()=>{const r=await call('auth/verify',form);setModal(null);setView(r.user.role==='teacher'?'studio':'teams');setNotice('登录成功');})}>验证并进入</Button></>}
 {modal?.type==='newCourse'&&<><label>教学期名称<Input placeholder="例如：2027 春季学期" value={form.term||''} onChange={e=>field('term',e.target.value)}/></label><p className="muted">{course?'复制当前课程模板，不复制学生和提交记录。':'预置四次实验，后续可上传文档与 Agent 对话修改。'}</p><Button disabled={busy} onClick={()=>act(async()=>{const r=await call('courses',{copyId:course?.id,term:form.term||'2026 秋季学期'});setSelected(r.id);setModal(null);setView('studio');})}>创建草案</Button></>}
 {modal?.type==='join'&&<><label>课程邀请码<Input value={form.code||''} onChange={e=>field('code',e.target.value)}/></label><Button disabled={busy} onClick={()=>act(async()=>{const r=await call('join',{code:form.code});setSelected(r.id);setModal(null);setView('teams');})}>加入教学班</Button></>}
 {modal?.type==='createTeam'&&<><label>Team 名称<Input placeholder="给你们的小组起个名字" value={form.name||''} onChange={e=>field('name',e.target.value)}/></label><Button disabled={busy} onClick={()=>mutation('teams',{courseId:s.selected,name:form.name})}>创建并担任组长</Button></>}
 {modal?.type==='invite'&&<><label>同学的邮箱<Input type="email" value={form.email||''} onChange={e=>field('email',e.target.value)}/></label><p className="muted">对方接受后才会加入。未注册同学可以使用此邮箱注册后接受邀请。</p><Button disabled={busy} onClick={()=>mutation('invite',{teamId:modal.teamId,email:form.email},'邀请已创建，对方可在网站接受；邮件按连接状态投递。')}>发出邀请</Button></>}
 {modal?.type==='teamConfirm'&&<><p>{modal.label}？历史提交的成员快照不会改变。</p><Button disabled={busy} onClick={()=>mutation('team-action',modal)}>确认变更</Button></>}
 {modal?.type==='publish'&&<><div className="questions"><strong>本次变更</strong>{changes.map((name:string)=><p key={name}>{name}</p>)}<p>已发布的历史版本和提交不受影响。</p></div><p><strong>{draft.title}</strong> · v{course?.revision}</p><ul className="publish-list">{draft.experiments.map((e:Any)=><li key={e.id}><Check size={15}/>{e.title}<span>{e.rubric.reduce((n:number,r:Any)=>n+r.max,0)} 分</span></li>)}</ul>{draft.questions?.length>0&&<p className="dialog-error">还有 {draft.questions.length} 项待确认，请先回到对话补充。</p>}<Button disabled={busy||!!draft.questions?.length} onClick={()=>mutation('publish',{courseId:s.selected,revision:course.revision},'课程已发布，学生可开始实验')}>确认发布这个版本</Button></>}
 {modal?.type==='formal'&&<><p>{detail?.title} · {team?.name}</p><p className="muted">成员：{team?.members.map((m:Any)=>m.name).join('、')}</p><ul>{attachments.map(f=><li key={f.id}>{f.name}</li>)}</ul><Button disabled={busy} onClick={()=>mutation('submit',{teamId:team.id,experimentId:detail.id,mode:'formal',fileIds:attachments.map(f=>f.id),note:message},'正式提交已锁定，等待核验')}>锁定并提交</Button></>}
 {['answer','appeal'].includes(modal?.type)&&<><textarea className="text-area" rows={7} aria-label={modal.type==='answer'?'个人回答':'申诉理由'} value={form.content||''} onChange={e=>field('content',e.target.value)} placeholder={modal.type==='answer'?'逐题说明你的理解，引用你完成的具体文件或测试。':'指出评分项、具体证据及你认为需要复核的理由。'}/><Button disabled={busy} onClick={()=>mutation(modal.type,{submissionId:modal.submissionId,content:form.content})}>提交</Button></>}
 {modal?.type==='review'&&<><label>复核理由<textarea className="text-area" value={form.reason||''} onChange={e=>field('reason',e.target.value)}/></label>{modal.sub?.report?.items.map((i:Any)=><label className="score-edit" key={i.id}>{i.id}<Input type="number" step="0.5" min="0" value={form['score_'+i.id]??i.score} onChange={e=>field('score_'+i.id,e.target.value)}/></label>)}<p className="muted">调整将保留原始评分和本次理由；默认保留 Agent 给出的证据。</p><Button disabled={busy} onClick={()=>mutation('grade-action',{submissionId:modal.sub.id,appealId:modal.appealId,reason:form.reason,report:{...modal.sub.report,items:modal.sub.report.items.map((i:Any)=>({...i,score:Number(form['score_'+i.id]??i.score)}))}},'复核记录已保存，成绩已发布')}>保存并发布成绩</Button></>}
 {modal?.type==='pair'&&<><p>新凭据会使旧连接器凭据失效。模型账号和 SMTP 密码只配置在你自己的连接器机器上。</p><Button disabled={busy} onClick={()=>act(async()=>{const r=await call('pair',{});setToken(r.token);setModal(null);})}>生成新凭据</Button></>}
 {modal?.type==='settings'&&<><label>小组人数上限<Input type="number" min="1" max="16" value={form.maxSize||4} onChange={e=>field('maxSize',e.target.value)}/></label><label>正式提交截止时间<Input type="datetime-local" value={form.deadline||''} onChange={e=>field('deadline',e.target.value)}/></label><label className="switch-label">通过校准后自动发布正常成绩<Switch checked={!!form.autoPublish} onCheckedChange={v=>field('autoPublish',v)}/></label><Button disabled={busy} onClick={()=>mutation('course-settings',{courseId:s.selected,...form})}>保存规则</Button></>}
 <DialogFooter><Button variant="ghost" disabled={busy} onClick={()=>setModal(null)}>关闭</Button></DialogFooter></DialogContent></Dialog></SidebarProvider>
}
